// points 

// js is single thread language means synchronized lauguage means thread safe environment 

// {js code } --> global Execution context (GEC) <-- refered by this keyword 

// in browers GEC is window

/**
 *  there are mainly two type of execution context but if you go indepth there are three 
 * 1 . Global Execution context
 * 2. Function/Functional Execution context
 * 3.eval execution context : - it is property of global Object
 */

/**
 * {js code} -> Memory creation Phase => memory allocation is done in this phase for variable  this phase also called  creation phase 
 *           -> Execution phase => execution is done in this phase 
 * 
*/